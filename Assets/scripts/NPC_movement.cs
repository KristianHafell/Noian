using UnityEngine;

public class NPC_movement : MonoBehaviour
{
    public Rigidbody2D NPC_rigidbody

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
